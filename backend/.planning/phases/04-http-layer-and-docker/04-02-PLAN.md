---
phase: 04-http-layer-and-docker
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
  - .env.example
autonomous: false
requirements: [INFRA-02]

must_haves:
  truths:
    - "docker compose up starts API, PostgreSQL, and Redis containers"
    - "PostgreSQL container passes pg_isready healthcheck"
    - "Redis container passes redis-cli ping healthcheck"
    - "API container starts only after both dependencies are healthy"
    - "API health endpoint is reachable at localhost:3000/api/health from the host"
    - ".env.example contains Docker-network-compatible connection strings as comments"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: TypeScript compile then production image"
      contains: "node:24-alpine"
    - path: "docker-compose.yml"
      provides: "Three-service stack with healthchecks"
      contains: "service_healthy"
    - path: ".dockerignore"
      provides: "Excludes node_modules, dist, .planning from Docker context"
      contains: "node_modules"
    - path: ".env.example"
      provides: "Updated env template with Docker-network comments"
      contains: "postgres:5432"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build: . on api service"
      pattern: "build:\\s*\\."
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file: .env on api service"
      pattern: "env_file"
---

<objective>
Create Dockerfile with multi-stage build, docker-compose.yml with API + PostgreSQL + Redis services and healthchecks, .dockerignore, and update .env.example with Docker-network-aware values.

Purpose: Enable the full stack to run locally via a single `docker compose up` command with proper startup ordering via healthchecks.
Output: Docker configuration files ready for local development.
</objective>

<execution_context>
@/home/karl/.claude/get-shit-done/workflows/execute-plan.md
@/home/karl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-http-layer-and-docker/04-CONTEXT.md
@.planning/phases/04-http-layer-and-docker/04-RESEARCH.md
@.planning/phases/04-http-layer-and-docker/04-01-SUMMARY.md

@package.json
@tsconfig.json
@src/config.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile, docker-compose.yml, .dockerignore, and .env.example update</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore, .env.example</files>
  <read_first>
    - package.json (scripts.build, scripts.start, dependencies)
    - tsconfig.json (outDir: dist, rootDir: src)
    - src/config.ts (REQUIRED_ENV_VARS, config.hostname default)
    - .env.example (current state)
    - .planning/phases/04-http-layer-and-docker/04-RESEARCH.md (Docker patterns, pitfalls)
  </read_first>
  <action>
**Create `Dockerfile`:**

```dockerfile
# Stage 1: build
FROM node:24-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY tsconfig.json ./
COPY src ./src
RUN npm run build

# Stage 2: production
FROM node:24-alpine AS production
WORKDIR /app
ENV NODE_ENV=production
COPY package*.json ./
RUN npm ci --omit=dev
COPY --from=build /app/dist ./dist
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

Key points:
- `package*.json` copied first for layer caching -- dependency layer only invalidated on lockfile change
- `npm ci` (not `npm install`) for deterministic builds
- `--omit=dev` in production stage to skip devDependencies (ts-jest, nodemon, etc.)
- `ENV NODE_ENV=production` as safe default; docker-compose .env overrides for local dev
- `EXPOSE 3000` documents the port but does not publish it

**Create `.dockerignore`:**

```
node_modules
dist
.planning
.git
.husky
.env
*.md
```

**Create `docker-compose.yml`:**

```yaml
services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ethereum_api
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    ports:
      - "6379:6379"

  api:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      HOSTNAME: "0.0.0.0"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  pgdata:
```

Key points:
- `HOSTNAME: "0.0.0.0"` explicitly set in api environment block to override kernel's HOSTNAME env var (Pitfall 5 from RESEARCH)
- `env_file: .env` injects all app env vars from .env file (Pitfall 4 from RESEARCH)
- `depends_on` with `condition: service_healthy` ensures Postgres and Redis are ready before API starts
- `pgdata` named volume persists database across container restarts
- API healthcheck uses wget (available in alpine) to hit /api/health
- `start_period: 15s` gives API time to initialize DataSource before first healthcheck

**Update `.env.example`:**

```bash
HOSTNAME=0.0.0.0
PORT=3000
LOG_LEVEL=info
NODE_ENV=development

# Local development (host machine)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ethereum_api
REDIS_URL=redis://localhost:6379

# Docker Compose (uncomment these, comment out the above)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ethereum_api
# REDIS_URL=redis://redis:6379

ETHERSCAN_API_KEY=your_api_key_here
ETHERSCAN_BASE_URL=https://api.etherscan.io/api
```

The Docker Compose lines use service names (`postgres`, `redis`) as hostnames because Docker Compose networking resolves service names to container IPs.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile uses multi-stage build with node:24-alpine, npm ci, npm run build, and --omit=dev in production stage
    - docker-compose.yml defines 3 services: postgres (17-alpine), redis (7-alpine), api (build: .)
    - Postgres has pg_isready healthcheck, Redis has redis-cli ping healthcheck
    - API has depends_on with condition: service_healthy for both
    - API environment block sets HOSTNAME=0.0.0.0 to avoid kernel collision
    - .dockerignore excludes node_modules, dist, .planning, .git
    - .env.example has Docker-network connection strings as comments
    - TypeScript still compiles (npm run build)
  </acceptance_criteria>
  <done>All Docker configuration files created, .env.example updated, TypeScript compilation still succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Docker Compose full stack boot</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
Human verifies the Docker Compose stack boots correctly and serves traffic. All Docker files were created in Task 1. This checkpoint confirms the full stack works end-to-end.
  </action>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <what-built>
    Dockerfile with multi-stage TypeScript build, docker-compose.yml with API + PostgreSQL + Redis and healthchecks, .dockerignore, and updated .env.example.
  </what-built>
  <how-to-verify>
    1. Create a `.env` file from `.env.example` with a real Etherscan API key (or keep the placeholder for health check only):
       ```
       cp .env.example .env
       # Edit .env: uncomment Docker DATABASE_URL and REDIS_URL, comment out localhost versions
       ```
    2. Run `docker compose up --build -d`
    3. Wait ~20 seconds for healthchecks to pass
    4. Run `docker compose ps` -- all 3 services should show "healthy" status
    5. Run `curl http://localhost:3000/api/health` -- should return `{"status":"ok"}`
    6. (Optional) If you have a real Etherscan API key in .env:
       `curl http://localhost:3000/api/ethereum/0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045`
       Should return the SuccessEnvelope with gas price, block number, and balance
    7. Run `docker compose down` to clean up
  </how-to-verify>
  <done>Docker Compose boots all 3 services healthy, API responds at localhost:3000/api/health</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` -- TypeScript compiles (Dockerfile and docker-compose.yml are not TS but the project must still compile)
2. `docker compose config` -- validates docker-compose.yml syntax (if Docker is available)
3. Manual: `docker compose up --build -d && docker compose ps` shows 3 healthy services
4. Manual: `curl localhost:3000/api/health` returns `{"status":"ok"}`
</verification>

<success_criteria>
- Dockerfile builds a production image with compiled TypeScript in dist/
- docker-compose.yml starts PostgreSQL, Redis, and API with healthcheck ordering
- API container is reachable at localhost:3000
- GET /api/health returns 200 with { status: "ok" } from Docker container
- .env.example documents both local and Docker connection strings
</success_criteria>

<output>
After completion, create `.planning/phases/04-http-layer-and-docker/04-02-SUMMARY.md`
</output>
