---
phase: 06-transfer-and-boundary-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/RoxasToken.test.ts]
autonomous: true
requirements: [TEST-06, TEST-07, TEST-08, TEST-09]

must_haves:
  truths:
    - "transfer() between two addresses succeeds and emits Transfer event"
    - "transfer() from address with zero balance reverts with ERC20InsufficientBalance"
    - "approve() sets allowance and emits Approval event"
    - "transferFrom() moves tokens on behalf of owner and emits Transfer event (not Approval)"
    - "Minting exactly to cap succeeds, any further mint reverts with ERC20ExceededCap"
  artifacts:
    - path: "test/RoxasToken.test.ts"
      provides: "Transfer, approval, and cap boundary tests"
      contains: "describe(\"Transfers\")"
    - path: "test/RoxasToken.test.ts"
      provides: "Approval flow tests"
      contains: "describe(\"Approvals\")"
  key_links:
    - from: "test/RoxasToken.test.ts"
      to: "contracts/RoxasToken.sol"
      via: "ethers.deployContract('RoxasToken') in deployFixture"
      pattern: "deployContract.*RoxasToken"
    - from: "test/RoxasToken.test.ts Transfers describe"
      to: "ERC20.transfer()"
      via: "token.transfer() calls and event assertions"
      pattern: "token\\.transfer\\("
    - from: "test/RoxasToken.test.ts Approvals describe"
      to: "ERC20.approve() + ERC20.transferFrom()"
      via: "token.approve() then token.connect().transferFrom()"
      pattern: "token\\.approve\\(|token\\.connect.*transferFrom\\("
---

<objective>
Add transfer, approval, and cap boundary tests to the existing test suite, covering TEST-06 through TEST-09.

Purpose: Complete the test coverage for all ERC-20 transfer and approval flows plus cap edge cases, so the contract is fully verified before Sepolia deployment.
Output: Extended test/RoxasToken.test.ts with new "Transfers" and "Approvals" describe blocks, all tests passing alongside existing Phase 5 tests.
</objective>

<execution_context>
@/home/karl/.claude/get-shit-done/workflows/execute-plan.md
@/home/karl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-transfer-and-boundary-tests/06-CONTEXT.md
@.planning/phases/06-transfer-and-boundary-tests/06-RESEARCH.md
@.planning/phases/05-deployment-and-minting-tests/05-01-SUMMARY.md
@test/RoxasToken.test.ts
@contracts/RoxasToken.sol

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From test/RoxasToken.test.ts (existing fixtures to reuse):
```typescript
// Lines 1-5: Top-level imports and HH3 ESM pattern
import { expect } from "chai";
import { network } from "hardhat";
import { anyValue } from "@nomicfoundation/hardhat-ethers-chai-matchers/withArgs";
const { ethers, networkHelpers } = await network.connect();

// Lines 7-11: deployFixture -- provides owner (1M RXS), user1 (0), user2 (0)
async function deployFixture() {
  const [owner, user1, user2] = await ethers.getSigners();
  const token = await ethers.deployContract("RoxasToken");
  return { token, owner, user1, user2 };
}

// Lines 13-33: nearCapFixture -- provides token near 10M cap, array of signers
async function nearCapFixture() { /* ... fills supply to within 1000 RXS of cap ... */ }
```

From contracts/RoxasToken.sol (relevant ERC-20 behavior):
```solidity
// Inherited from OZ ERC20: transfer(), approve(), transferFrom(), allowance(), balanceOf()
// Inherited from OZ ERC20Capped: cap(), _update() override with ERC20ExceededCap
// OZ v5 custom errors (from draft-IERC6093.sol):
//   ERC20InsufficientBalance(address sender, uint256 balance, uint256 needed)
//   ERC20InsufficientAllowance(address spender, uint256 allowance, uint256 needed)
//   ERC20ExceededCap(uint256 increasedSupply, uint256 cap)
// CRITICAL: transferFrom() does NOT emit Approval event in OZ v5 (emitEvent=false at line 302)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transfer tests (TEST-06, TEST-07)</name>
  <files>test/RoxasToken.test.ts</files>
  <read_first>
    - test/RoxasToken.test.ts (full file -- understand structure and existing describe blocks)
    - contracts/RoxasToken.sol (contract under test)
    - .planning/phases/06-transfer-and-boundary-tests/06-RESEARCH.md (patterns 1-2, pitfalls 2-3)
  </read_first>
  <behavior>
    - Test: transfer 100 RXS from owner to user1 succeeds, user1 balance == 100 RXS, owner balance == 999900 RXS, Transfer(owner, user1, 100 RXS) event emitted
    - Test: transfer 50 RXS from owner to user1, then transfer 50 RXS from user1 to user2 succeeds (tokens are freely transferable after receipt)
    - Test: user1 (0 balance) attempts transfer of 1 RXS to user2 reverts with ERC20InsufficientBalance(user1.address, 0n, parseEther("1"))
    - Test: owner attempts transfer of amount exceeding their balance reverts with ERC20InsufficientBalance
  </behavior>
  <action>
Add a `describe("Transfers", function () { ... })` block inside the top-level `describe("RoxasToken")`, after the existing "Cap enforcement" block (after line 188, before the closing `});` on line 189).

Tests to write (all reuse `deployFixture` via `networkHelpers.loadFixture`):

1. "should transfer tokens and emit Transfer event" (TEST-06):
   - Load deployFixture, set amount = parseEther("100")
   - `await expect(token.transfer(user1.address, amount)).to.emit(token, "Transfer").withArgs(owner.address, user1.address, amount)`
   - Assert `balanceOf(user1) == amount`
   - Assert `balanceOf(owner) == parseEther("1000000") - amount`

2. "should allow recipient to transfer received tokens" (TEST-06 secondary):
   - Load deployFixture, transfer 50 RXS from owner to user1
   - Then `token.connect(user1).transfer(user2.address, parseEther("50"))`
   - Assert Transfer event emitted, user2 balance == 50 RXS, user1 balance == 0

3. "should revert transfer on insufficient balance" (TEST-07):
   - Load deployFixture (user1 has 0 balance)
   - `await expect(token.connect(user1).transfer(user2.address, parseEther("1"))).to.be.revertedWithCustomError(token, "ERC20InsufficientBalance").withArgs(user1.address, 0n, parseEther("1"))`

4. "should revert when transfer amount exceeds balance" (TEST-07 secondary):
   - Load deployFixture, owner has 1M RXS
   - `await expect(token.transfer(user1.address, parseEther("1000001"))).to.be.revertedWithCustomError(token, "ERC20InsufficientBalance").withArgs(owner.address, parseEther("1000000"), parseEther("1000001"))`

Use ethers v6 BigInt throughout. Follow exact patterns from Phase 5 tests (loadFixture, expect().to.emit, expect().to.be.revertedWithCustomError).

Run `npx hardhat test test/RoxasToken.test.ts` -- all 15 existing + 4 new tests must pass.
  </action>
  <verify>
    <automated>cd /home/karl/Documents/repository/karlmarx-roxas-mobiledev/smartcontract && npx hardhat test test/RoxasToken.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - "Transfers" describe block exists in test file
    - 4 transfer tests pass: transfer success + event, recipient re-transfer, insufficient balance revert, exceeds balance revert
    - All 15 existing Phase 5 tests still pass
    - Total passing: 19 tests
  </acceptance_criteria>
  <done>transfer() succeeds between addresses with correct balance updates and Transfer event emission (TEST-06). transfer() reverts with ERC20InsufficientBalance when sender lacks funds (TEST-07). All 19 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add approval/transferFrom and cap boundary tests (TEST-08, TEST-09)</name>
  <files>test/RoxasToken.test.ts</files>
  <read_first>
    - test/RoxasToken.test.ts (re-read after Task 1 modifications)
    - .planning/phases/06-transfer-and-boundary-tests/06-RESEARCH.md (pattern 3, pattern 4, pitfall 1, pitfall 4, pitfall 5)
  </read_first>
  <behavior>
    - Test: owner approves user1 for 500 RXS, Approval(owner, user1, 500) event emitted, allowance(owner, user1) == 500
    - Test: after approval, user1 calls transferFrom(owner, user2, 500), Transfer(owner, user2, 500) emitted (NOT Approval), user2 balance == 500, allowance drops to 0
    - Test: transferFrom without approval reverts with ERC20InsufficientAllowance(user1, 0, amount)
    - Test: cap boundary -- mint exactly to cap, totalSupply == cap, next mint reverts with ERC20ExceededCap (traceability test for TEST-09)
  </behavior>
  <action>
Add a `describe("Approvals", function () { ... })` block inside the top-level `describe("RoxasToken")`, after the "Transfers" block added in Task 1.

Tests to write (all reuse existing fixtures):

1. "should approve spender and emit Approval event" (TEST-08 step 1):
   - Load deployFixture, amount = parseEther("500")
   - `await expect(token.approve(user1.address, amount)).to.emit(token, "Approval").withArgs(owner.address, user1.address, amount)`
   - Assert `allowance(owner, user1) == amount`

2. "should allow transferFrom after approval" (TEST-08 step 2):
   - Load deployFixture, amount = parseEther("500")
   - `token.approve(user1.address, amount)` (no event assertion needed, covered above)
   - `await expect(token.connect(user1).transferFrom(owner.address, user2.address, amount)).to.emit(token, "Transfer").withArgs(owner.address, user2.address, amount)`
   - Assert `balanceOf(user2) == amount`
   - Assert `balanceOf(owner) == parseEther("1000000") - amount`
   - Assert `allowance(owner, user1) == 0n`
   - Do NOT assert Approval event on transferFrom (OZ v5 suppresses it)

3. "should revert transferFrom without approval" (TEST-08 supporting):
   - Load deployFixture
   - `await expect(token.connect(user1).transferFrom(owner.address, user2.address, parseEther("1"))).to.be.revertedWithCustomError(token, "ERC20InsufficientAllowance").withArgs(user1.address, 0n, parseEther("1"))`

Then add a dedicated cap boundary test inside the existing `describe("Cap enforcement")` block for TEST-09 traceability:

4. "should handle exact cap boundary: mint to cap then revert" (TEST-09):
   - Load nearCapFixture
   - Calculate remaining = cap - totalSupply
   - `await networkHelpers.time.increase(61)` (allow minting after fixture loop)
   - If remaining > 0, mint exactly remaining with signers[0]
   - Assert `totalSupply() == cap()`
   - `await networkHelpers.time.increase(61)`
   - Assert `token.connect(signers[1]).mint(1n)` reverts with `ERC20ExceededCap`
   - This test mints the minimum possible (1 wei) to prove even the smallest mint fails at cap

Use ethers v6 BigInt. CRITICAL: Do NOT assert Approval event on transferFrom calls.

Run `npx hardhat test test/RoxasToken.test.ts` -- all 19 existing + 4 new tests must pass.
  </action>
  <verify>
    <automated>cd /home/karl/Documents/repository/karlmarx-roxas-mobiledev/smartcontract && npx hardhat test test/RoxasToken.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - "Approvals" describe block exists in test file
    - 3 approval tests pass: approve + event, transferFrom after approval, transferFrom revert without approval
    - 1 cap boundary test passes in existing "Cap enforcement" block
    - No Approval event asserted on any transferFrom call
    - All 19 tests from Task 1 (15 Phase 5 + 4 Task 1) still pass
    - Total passing: 23 tests
  </acceptance_criteria>
  <done>approve() + transferFrom() flow works correctly end-to-end with proper event assertions (TEST-08). Cap boundary explicitly tested: mint exactly to cap succeeds, next mint of even 1 wei reverts (TEST-09). All 23 tests pass with `npx hardhat test` exit code 0.</done>
</task>

</tasks>

<verification>
Run the full test suite and confirm:
1. `npx hardhat test test/RoxasToken.test.ts` exits with 0
2. 23 total passing tests (15 Phase 5 + 4 transfer + 3 approval + 1 cap boundary)
3. No pending or failing tests
4. describe blocks present: "Deployment", "Minting", "Cap enforcement", "Transfers", "Approvals"
</verification>

<success_criteria>
- TEST-06: At least 2 tests verify transfer() succeeds with correct balances and Transfer event
- TEST-07: At least 2 tests verify transfer() reverts with ERC20InsufficientBalance on insufficient funds
- TEST-08: At least 3 tests verify the full approve + transferFrom flow including event assertions and revert on no approval
- TEST-09: Dedicated cap boundary test proves mint-to-cap then revert behavior (supplements existing Phase 5 cap tests)
- All 23 tests pass, `npx hardhat test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-transfer-and-boundary-tests/06-01-SUMMARY.md`
</output>
